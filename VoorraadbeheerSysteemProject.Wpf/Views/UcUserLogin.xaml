<UserControl x:Class="VoorraadbeheerSysteemProject.Wpf.Views.UcUserLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#1E1E1E">
        <Border Background="White" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Login" FontSize="22" FontWeight="Bold" Margin="0 0 0 20" HorizontalAlignment="Center"/>

                <TextBox Margin="0 5" Height="30" Padding="5" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                <PasswordBox Margin="0 5" Height="30" Padding="5"/>

                <Button Content="Sign In" Margin="0 15 0 0" Background="#007BFF" Foreground="White"
                    Height="35" FontWeight="SemiBold" Command="{Binding LoginCommand}"/>

                <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0 10 0 0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>